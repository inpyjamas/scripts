export async function init(): Promise<void> {
  // TODO: ask user for input
  // - eslintrc.js
  // - prettier.config.js
  // - nodemon.json
  // - add scripts to package.json (if exists)
  // dev
  // test
  // start
}

if (require.main === module) {
  init().catch(console.error);
}
